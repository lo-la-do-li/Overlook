class HotelManagement {
  constructor(allCustomers, allRooms, allBookings) {
    this.allCustomers = allCustomers || [],
    this.allRooms = allRooms || [],
    this.allBookings = allBookings || []
  }
}

export default HotelManagement;
